// Core
import { Layout } from '@/components';
import { FC } from 'react';

const Login: FC = () => {
    return( 
        <Layout>
            <div>Login</div>
        </Layout>
    );
};

export default Login;