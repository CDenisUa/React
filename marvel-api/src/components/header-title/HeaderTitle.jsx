import {
    TitleRow,
    MainTitle,
    Query,
} from "./styles";

const HeaderTitle = () => (
    <TitleRow>
        <MainTitle>
            <span>Marvel</span> information portal
        </MainTitle>
        <Query>Characters / Comics</Query>
    </TitleRow>
);

export default HeaderTitle;