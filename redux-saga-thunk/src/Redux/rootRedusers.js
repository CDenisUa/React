const initialState = {
    counter: 0
}
const rootRedusers = (state = initialState, action) => {
    switch(action.type){
        
        default:
            return state
    }
}

export default rootRedusers;